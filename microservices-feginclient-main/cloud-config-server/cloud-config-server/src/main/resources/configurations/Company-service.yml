eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 9099
spring:
  application:
    name: Company-service
  datasource:
    url: jdbc:mysql://localhost:3306/project1
    username: root
    password: pass@word1
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true